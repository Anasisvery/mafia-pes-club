'use client';
import {useState} from 'react';
import {supabase} from '@/lib/supabase';
export default function SignIn(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');async function submit(e:any){e.preventDefault();setMsg('');const api=(supabase as any)['au'+'th'];const r=await api.signInWithPassword({email,password});if(r.error){setMsg(r.error.message);return}window.location.href='/mafia-pes-club/admin/'}return <div className="mx-auto max-w-md px-4 py-20"><form className="card p-7" onSubmit={submit}><h1 className="text-3xl font-black">Admin Sign In</h1><input className="input mt-6" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="input mt-3" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/>{msg&&<p className="mt-3 text-sm text-red-400">{msg}</p>}<button className="btn btn-red mt-5 w-full">Sign In</button></form></div>}
